package com.htmlism.spawningpool

/**
  * Chromosomes are often implemented as collections of some type. Included in this package are helper traits that
  * leverage this pattern.
  *
  * The element type of the chromosome can either be continuous (e.g. `Int`, `Double`) or discrete (i.e. elements from a
  * sequence).
  */
package object combinatorics
